// let youtubeURL="https://youtube.com";
// let ventana= window.open(youtubeURL);
// ventana.close();

// prompt("introduce tu dato, siempre lo recibe y lo vuelve un string");

// confirm("Pregunta y nos da dos opciones si o no, devuelve un booleano");


// window.screen; es un objeto que trabaja con la pantalla
// window.screenLeft; dice la distancia de la ventana con la parte izquierda de la pantalla


// print(); con el print permite imprimir
// closed es para parar la carga de una ventana  

// scrollX; scroll en x 
// scrollY; scroll en y

// nos dice la locasion
const herf=window.location.href;

console.log(herf);
console.log(window.location.pathname);